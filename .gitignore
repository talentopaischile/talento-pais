# Credenciales — NUNCA subir
scripts/google_credentials.json
*.env

# Datos procesados (se regeneran en cada run)
datos/raw/*.json
datos/procesados/*.json

# CSV grande del Mineduc (903MB, no cabe en GitHub)
datos/raw/*.csv

# Excluir JSONs crudos del scraper (se regeneran cada semana)
# EXCEPTO carreras_estrategicas.json que va en el repo
datos/raw/*.json
!datos/raw/carreras_estrategicas.json

# Logs
*.log

# Python
__pycache__/
*.pyc
.venv/
